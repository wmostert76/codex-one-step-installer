# Release Checklist

Use this checklist for every release or change that goes to users.

- Bump `$ScriptVersion` in `codex-one-step-install.ps1`.
- Confirm installer still runs from GitHub Pages short link.
- Verify Node.js, Python, and Codex CLI installs complete.
- Smoke-test `codex --version`.
- Update `CHANGELOG.md`.
